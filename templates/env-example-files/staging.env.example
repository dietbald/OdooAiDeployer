# Local env for `staging`. AI workspace should keep this empty or only
# contain a READ-ONLY Odoo user. Real deploys to staging happen in CI.
ODOO_URL=https://your-staging-instance.odoo.com
ODOO_DB=your-staging-db
ODOO_USERNAME=read-only@example.com
ODOO_PASSWORD=
