# Local env for `production`. Should never exist on the AI workspace.
# Real deploys to production happen in CI with TJ approval.
ODOO_URL=https://your-prod-instance.odoo.com
ODOO_DB=your-prod-db
ODOO_USERNAME=admin@example.com
ODOO_PASSWORD=
